engine: "1"
# item 9 regression check
pipeline:
  name: hello
stages:
  greet:
    run: |
      echo "Hello from LabHit CI"
      test "$MY_SECRET" = "hello123" && echo "secret present (value redacted in logs)"
