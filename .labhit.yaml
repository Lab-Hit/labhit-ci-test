engine: "1"
pipeline:
  name: hello
stages:
  greet:
    run: |
      echo "secret length is ${#MY_SECRET}"
      echo "raw would be: $MY_SECRET"
      test "$MY_SECRET" = "hello123" && echo PIPELINE_OK
